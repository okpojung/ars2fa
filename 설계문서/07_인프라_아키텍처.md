# 07. 인프라 아키텍처

## 1. 목적

이 문서는 `ars2fa` 발신 ARS 인증 API 신청 사이트와 인증 API Gateway를 운영하기 위한 인프라 구성을 정의한다.

구성 방향은 `easymindmap`의 IDC 기반 인프라 문서(`docs/90-architecture/infra-architecture.md`)를 참조한다.

- IDC 내부망: `192.168.94.0/24`
- 외부 진입: Cloudflare DNS -> FortiGate -> Nginx Proxy Manager(NPM)
- 애플리케이션: App VM, DB/Supabase VM, Redis VM, Worker VM, 개발 VM 분리
- 원격 운영: FortiGate IPSec VPN, SSH 2222, 내부 관리 도구 IP 제한

## 2. 도메인 정책

| 구분 | URL | 공개 여부 | 설명 |
|---|---|---|---|
| 사용자 포털 | `https://ars2fa.baro.me` | 공개 | 회원 가입, API 신청, API Key 관리, 인증 현황 조회 |
| 관리자 콘솔 | `https://ars2fa.baro.me/admin` | 제한 권장 | 관리자 로그인, 신청 승인/반려, API Key 운영 |
| 업무 API | `https://ars2fa.baro.me/api/*` | 공개/인증 필수 | 포털/관리자 화면용 API |
| ARS 인증 API | `https://ars2fa.baro.me/v1/ars/*` | 공개/Key 필수 | 모바일 앱 및 `ars_auth` 위젯 호출 |
| 헬스체크 | `https://ars2fa.baro.me/health` | 공개 가능 | NPM/운영 모니터링용 상태 확인 |

관리자 콘솔은 별도 서브도메인 대신 `ars2fa.baro.me/admin` 경로를 사용한다. 따라서 SSL 인증서, 쿠키, CORS, SameSite 정책은 단일 도메인 기준으로 단순화한다.

## 3. 전체 인프라 구성도

```text
[인터넷]
   │
   ▼
[Cloudflare DNS]
  ars2fa.baro.me → 218.232.94.74 (A 레코드)
  Proxy: DNS only 권장
  ※ NPM이 Let's Encrypt SSL을 처리
   │
   ▼
[FortiGate 방화벽]
  공인 IP  : 218.232.94.74
  공개 포트: 80, 443
  운영 접속: IPSec VPN, 필요 시 RDP/SSH는 허용 IP 제한
   │
   ▼ NAT → 192.168.94.0/24
   │
   ├────────────────────────────────────────┐
   ▼                                        ▼
[NPM 서버]                            [L4 스위치 - 확장 시]
  내부 IP: 192.168.94.74                 VIP: 192.168.94.2
  역할: Reverse Proxy + SSL              역할: App VM 다중화 시 내부 부하분산
  도메인: ars2fa.baro.me
   │
   ├── /, /admin       → VM-02 Frontend :5173
   ├── /api/*          → VM-02 Backend  :3000
   ├── /v1/ars/*       → VM-02 Backend  :3000
   └── /health         → VM-02 Backend  :3000
```

## 4. IDC 내부 VM 통신 구조

```text
NPM (192.168.94.74)
  ├──[443→5173]──▶ VM-02 ars-app Frontend/Admin SPA (192.168.94.122)
  └──[443→3000]──▶ VM-02 ars-app Backend/API Gateway (192.168.94.122)

VM-02 ars-app (192.168.94.122)
  ├──▶ VM-03 ars-db    :5432  PostgreSQL
  ├──▶ VM-03 ars-db    :9999  Supabase Auth 또는 인증 보조 서비스
  ├──▶ VM-03 ars-db    :9000  Supabase Storage
  ├──▶ VM-04 ars-redis :6379  Redis 캐시/Rate Limit/Queue
  └──▶ 외부 ARS 서버          barocall.baro.so install/confirm/recheck

VM-05 ars-worker (192.168.94.125)
  ├──▶ VM-03 ars-db    :5432  PostgreSQL
  ├──▶ VM-04 ars-redis :6379  Queue
  ├──▶ 외부 ARS 서버          장애 재처리/집계 보조
  └──▶ 메일/SMS Provider      승인/장애 알림

VM-DEV ars-dev (192.168.94.120)
  └── 개발 전체 스택 (Docker: DB + Redis + Backend + Frontend)

운영자 PC
  └──[FortiGate IPSec VPN]──▶ IDC 192.168.94.0/24
      ├── SSH  → 각 VM :2222
      ├── NPM 관리 UI → 192.168.94.74:81
      └── DB/Admin 도구 → 내부 IP만 허용
```

## 5. VM 배치 권장안

Easymindmap 인프라와 동일하게 NVMe 용량과 CPU 성능이 높은 ESXi 호스트에 I/O 집약 VM을 배치한다.

| VM | IP | Hostname | ESXi 권장 | 역할 |
|---|---|---|---|---|
| VM-DEV | `192.168.94.120` | `ars-dev` | `192.168.94.11` | 개발 서버, Docker 기반 통합 개발 스택 |
| VM-02 | `192.168.94.122` | `ars-app` | `192.168.94.12` | Frontend/Admin SPA, Backend API Gateway |
| VM-03 | `192.168.94.123` | `ars-db` | `192.168.94.11` | PostgreSQL/Supabase, 인증/신청 데이터 저장 |
| VM-04 | `192.168.94.124` | `ars-redis` | `192.168.94.80` | Redis, Rate Limit, Queue, 세션 캐시 |
| VM-05 | `192.168.94.125` | `ars-worker` | `192.168.94.12` | 통계 집계, 알림 발송, 로그 후처리 |

초기 운영은 VM-02/03/04 중심으로 시작하고, 통계 집계와 알림 작업이 늘어나면 VM-05 Worker를 활성화한다.

## 6. VM 스펙 계획

### 6.1 초기 운영

| VM | vCPU | RAM | Disk | 설명 |
|---|---:|---:|---:|---|
| VM-DEV | 8 | 16 GB | 200 GB | 개발 전체 스택 |
| VM-02 App | 4 | 8 GB | 80 GB | Node/NestJS 또는 동등 Backend, Frontend preview/static serving |
| VM-03 DB | 4 | 16 GB | 200 GB | PostgreSQL, 인증 로그 저장 |
| VM-04 Redis | 2 | 4 GB | 30 GB | 캐시, rate limit, queue |
| VM-05 Worker | 2 | 4 GB | 50 GB | 통계/알림/로그 후처리 |

### 6.2 확장 기준

| 증가 요인 | 확장 방향 |
|---|---|
| 인증 API 호출량 증가 | VM-02 Backend PM2 cluster instance 증가, L4 VIP 또는 App VM 추가 |
| 인증 로그 증가 | VM-03 PostgreSQL RAM/NVMe 증설, 월별 파티셔닝, read replica 검토 |
| 통계/CSV 다운로드 증가 | VM-05 Worker 분리 및 큐 처리량 증가 |
| Key 검증/Rate Limit 부하 증가 | VM-04 Redis RAM 증설, Redis persistence/replica 검토 |

## 7. Cloudflare DNS 설정

| Type | Name | Content | Proxy |
|---|---|---|---|
| A | `ars2fa.baro.me` | `218.232.94.74` | DNS only 권장 |

주의사항:

- NPM이 Let's Encrypt 인증서를 직접 발급하므로 Cloudflare Proxy는 기본적으로 끈다.
- Cloudflare Proxy를 켜야 하는 경우 SSL 모드, 원본 인증서, client IP 전달 정책을 별도 검토한다.
- 관리자 콘솔은 `/admin` path 기반이므로 별도 DNS 레코드는 필요 없다.

## 8. Nginx Proxy Manager 라우팅

### 8.1 Proxy Host 기본 설정

| 항목 | 값 |
|---|---|
| Domain Names | `ars2fa.baro.me` |
| Scheme | `http` |
| Forward Hostname | `192.168.94.122` |
| Forward Port | `5173` |
| Websockets Support | 필요 시 활성화 |
| SSL | Let's Encrypt, Force SSL, HTTP/2, HSTS |

### 8.2 Advanced 라우팅 예시

```nginx
# Backend 업무 API
location /api/ {
    proxy_pass         http://192.168.94.122:3000/api/;
    proxy_http_version 1.1;
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_connect_timeout 10s;
    proxy_read_timeout    60s;
    limit_req zone=api burst=30 nodelay;
}

# ARS 인증 API - 모바일 앱 호출 경로
location /v1/ars/ {
    proxy_pass         http://192.168.94.122:3000/v1/ars/;
    proxy_http_version 1.1;
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_connect_timeout 10s;
    proxy_read_timeout    60s;
    limit_req zone=auth burst=10 nodelay;
}

# 헬스체크
location /health {
    proxy_pass         http://192.168.94.122:3000/health;
    proxy_set_header   Host $host;
}

# SPA 라우팅: /admin 새로고침도 프론트엔드로 전달
location / {
    proxy_pass         http://192.168.94.122:5173;
    proxy_http_version 1.1;
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
}

add_header X-Frame-Options        SAMEORIGIN;
add_header X-Content-Type-Options nosniff;
add_header Referrer-Policy        strict-origin-when-cross-origin;
```

### 8.3 NPM Rate Limit 전역 설정

```nginx
limit_req_zone $binary_remote_addr zone=api:10m  rate=60r/m;
limit_req_zone $binary_remote_addr zone=auth:10m rate=20r/m;
limit_req_zone $binary_remote_addr zone=admin:10m rate=30r/m;
```

애플리케이션 레벨에서도 API Key별 rate limit을 반드시 적용한다. NPM rate limit은 IP 기준 1차 방어로만 사용한다.

## 9. 애플리케이션 배포 구조

VM-02는 Frontend/Admin SPA와 Backend API Gateway를 함께 운영한다.

```text
/opt/ars2fa
  ├── apps/frontend      # 사용자 포털 + /admin 관리자 라우트
  ├── apps/backend       # 회원/API 신청/API Key/ARS Gateway
  ├── ecosystem.config.js
  └── deploy.sh
```

PM2 프로세스 예시:

| 프로세스 | 포트 | 설명 |
|---|---:|---|
| `ars2fa-api` | 3000 | Backend API, ARS Gateway |
| `ars2fa-frontend` | 5173 | 사용자 포털 및 관리자 SPA |

운영 환경 변수 예시:

```env
NODE_ENV=production
APP_URL=https://ars2fa.baro.me
ADMIN_URL=https://ars2fa.baro.me/admin
API_BASE_URL=https://ars2fa.baro.me/api
ARS_API_BASE_URL=https://ars2fa.baro.me/v1/ars
DATABASE_URL=postgresql://ars2fa:***@192.168.94.123:5432/ars2fa
REDIS_URL=redis://:***@192.168.94.124:6379
API_KEY_HASH_SECRET=***
PHONE_HASH_SECRET=***
LEGACY_ARS_BASE_URL=https://barocall.baro.so
```

## 10. 방화벽 및 포트 정책

| 서버 | IP | 포트 | 서비스 | 허용 대상 |
|---|---|---:|---|---|
| FortiGate | `218.232.94.74` | 80, 443 | HTTP/HTTPS | 전체 |
| FortiGate | `218.232.94.74` | 500, 4500 | IPSec VPN | 운영 허용 IP |
| NPM | `192.168.94.74` | 80, 443 | Reverse Proxy | FortiGate |
| NPM | `192.168.94.74` | 81 | NPM Admin UI | VPN/내부망만 |
| VM-02 | `192.168.94.122` | 3000 | Backend API | NPM만 |
| VM-02 | `192.168.94.122` | 5173 | Frontend SPA | NPM만 |
| VM-03 | `192.168.94.123` | 5432 | PostgreSQL | VM-02, VM-05, VM-DEV |
| VM-03 | `192.168.94.123` | 9999, 9000 | Supabase/Auth/Storage | VM-02, VM-05 |
| VM-04 | `192.168.94.124` | 6379 | Redis | VM-02, VM-05, VM-DEV |
| VM-05 | `192.168.94.125` | 2222 | SSH | VPN/운영자 |
| 각 VM | 내부 IP | 2222 | SSH | VPN/운영자 |

모든 VM은 기본 정책을 `deny incoming`, `allow outgoing`으로 두고 필요한 내부 통신만 허용한다.

## 11. 데이터 저장소 설계

### 11.1 PostgreSQL

저장 대상:

- 회원/회사/관리자 계정.
- API 신청 및 심사 이력.
- API Key 해시.
- 인증 요청 최신 상태.
- 인증 이벤트 로그.
- 감사 로그.

운영 권장:

- 인증 로그 테이블은 월별 파티셔닝을 검토한다.
- `auth_requests(api_key_id, requested_at)` 복합 인덱스를 유지한다.
- 전화번호 원문은 저장하지 않고 HMAC 해시와 마스킹값만 저장한다.
- 느린 쿼리 로그를 활성화한다.

### 11.2 Redis

사용 대상:

- API Key 검증 결과 단기 캐시.
- IP/API Key별 rate limit counter.
- 인증 현황 CSV 생성 작업 큐.
- 알림 발송 큐.
- 관리자 세션 또는 refresh token denylist.

## 12. 외부 연동

| 연동 대상 | 방향 | 용도 |
|---|---|---|
| 기존 ARS 서버 `barocall.baro.so` | VM-02 -> 외부 | install/confirm/recheck 중계 |
| 메일/SMS Provider | VM-02/VM-05 -> 외부 | 이메일 인증, 승인/반려, 장애 알림 |
| GitHub | VM-DEV/VM-02 -> 외부 | 배포 소스 pull |
| Cloudflare | 운영자 -> 외부 | DNS 관리 |

기존 ARS 서버 장애는 `ARS_PROVIDER_ERROR`로 표준화하고, 인증 이벤트 로그에 provider 응답 상태와 마스킹된 응답을 저장한다.

## 13. 백업 전략

| 대상 | 방식 | 주기 | 보관 |
|---|---|---|---|
| PostgreSQL | `pg_dump` gzip 백업 | 매일 새벽 | 상세 정책에 따라 30일 이상 |
| 인증 로그 집계 | 일별 통계 테이블 백업 | 매일 | 장기 보관 |
| 환경 변수/설정 | 암호화된 별도 저장소 | 변경 시 | 최신 + 주요 변경 이력 |
| NPM 설정 | NPM data 디렉터리 백업 | 변경 시/주기적 | 복구 가능 기간 |

복구 절차는 운영 리허설을 통해 검증해야 한다. 특히 API Key 원문은 저장하지 않으므로 DB 복구 후에도 기존 Key 원문 재노출은 불가능하다는 점을 운영 정책에 명시한다.

## 14. 모니터링

### 14.1 헬스체크

| 체크 | URL/대상 | 기준 |
|---|---|---|
| Public Web | `https://ars2fa.baro.me/health` | 200 OK |
| Backend API | VM-02 `localhost:3000/health` | 200 OK |
| PostgreSQL | VM-03 `5432` | connection 성공 |
| Redis | VM-04 `6379` | `PONG` |
| 기존 ARS 서버 | install/confirm synthetic test | timeout/5xx 감지 |

### 14.2 알림 지표

- 인증 API 5xx 비율 증가.
- 기존 ARS 서버 timeout 증가.
- `UNAUTHORIZED_API_KEY`, `APP_NOT_ALLOWED` 급증.
- API Key별 월 한도 80%/100% 도달.
- DB disk 사용량 80% 초과.
- Redis memory 사용량 80% 초과.
- Worker queue 적체.

## 15. 운영 보안

- 모든 공개 트래픽은 HTTPS로 강제한다.
- `/admin` 화면 접근은 관리자 로그인과 서버 권한 검사를 필수로 한다.
- 관리자 세션은 짧은 idle timeout과 재인증 정책을 적용한다.
- NPM Admin UI, DB Studio, SSH는 VPN/내부망에서만 접근한다.
- API Key와 휴대폰 번호는 로그에서 마스킹한다.
- 서버 간 secret은 `.env` 평문 배포 대신 별도 secret 관리 체계로 이전하는 것을 권장한다.
- 운영 배포 전 `ars_auth` 모바일 앱에서 debug log가 API Key를 출력하지 않는지 확인한다.

## 16. 배포 흐름

```text
main 브랜치 업데이트
  │
  ▼
VM-DEV 검증
  │
  ▼
VM-02 배포 스크립트 실행
  │
  ├── Backend install/build
  ├── Frontend install/build
  ├── DB migration 적용
  ├── PM2 reload
  └── /health 확인
```

배포 후 확인 항목:

1. `https://ars2fa.baro.me` 사용자 포털 접속.
2. `https://ars2fa.baro.me/admin` 관리자 로그인 화면 접속.
3. `https://ars2fa.baro.me/health` 정상 응답.
4. 테스트 API Key로 `/v1/ars/install` 검증.
5. 인증 현황 화면에 테스트 요청 로그 표시.

## 17. 확장 체크리스트

| 단계 | 조치 |
|---|---|
| 초기 운영 | 단일 VM-02 App, 단일 PostgreSQL, 단일 Redis |
| 트래픽 증가 | VM-02 PM2 cluster 증가, NPM rate limit 조정 |
| 로그 증가 | PostgreSQL 파티셔닝, 통계 집계 Worker 분리 |
| 장애 대응 강화 | App VM 추가, L4 VIP 활성화, PostgreSQL replica 검토 |
| 보안 강화 | 관리자 MFA, 앱 서명 검증, API Key rotation 자동화 |

## 18. 참조 차이점

Easymindmap 참조 인프라와 비교한 ars2fa 적용 차이는 다음과 같다.

| 항목 | Easymindmap | ars2fa |
|---|---|---|
| 대표 도메인 | `mindmap.ai.kr` | `ars2fa.baro.me` |
| 관리자 접근 | 별도 정의 없음 | `ars2fa.baro.me/admin` |
| 주요 Backend | Mindmap API/WebSocket/AI Worker | API 신청/Key 관리/ARS Gateway |
| 외부 연동 | OpenAI 등 AI API | 기존 ARS 서버, 메일/SMS Provider |
| 핵심 데이터 | 마인드맵/AI 작업 | API 신청, API Key, 인증 로그 |
| Rate Limit 기준 | API/IP 중심 | API Key/IP/앱명 중심 |
